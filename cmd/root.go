package cmd

import (
	"os"

	"github.com/spf13/cobra"
)

// rootCmd represents the base command when called without any subcommands
var rootCmd = &cobra.Command{
	Use:   "goctobot",
	Short: "Manage GitHub followers and following",
	Long: `A CLI tool to manage GitHub followers and following.

It helps you:
- Follow users that follow you but you don't follow back
- Unfollow users that don't follow you back
- Manage allow and deny lists`,
}

// Execute adds all child commands to the root command and sets flags appropriately.
// This is called by main.main(). It only needs to happen once to the rootCmd.
func Execute() {
	err := rootCmd.Execute()
	if err != nil {
		os.Exit(1)
	}
}

func init() {
	rootCmd.Flags().BoolP("toggle", "t", false, "Help message for toggle")
}
